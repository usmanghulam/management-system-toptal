import React, { Fragment } from 'react';

const ExportButton = () => {
	return (
		<Fragment>
			<button className="btn btn-dark">Export <i className="fa fa-download"></i></button>
		</Fragment>
	);
};

export default ExportButton;